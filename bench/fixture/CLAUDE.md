# shop — working rules
Small order service used as a review target. Python 3 stdlib only.
| Purpose | Command | Measured |
|---|---|---|
| FAST GATE | `python -m compileall -q shop && python -m unittest -q` | ~1 s |
