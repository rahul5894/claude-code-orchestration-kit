__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Task buckets: working notes for one task, nobody else needs them
.claude/scratch/

# Live MCP config carries API keys; commit the template, keep the real one local
.mcp.json

# Benchmark runs: clones, patches and scores stay local
bench/results/
bench/.venv/
